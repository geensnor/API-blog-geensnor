# Blog excerpt API

This PHP API reads markdown blog posts from the public GitHub repository
`geensnor/geensnor.nl` and returns a random three-sentence excerpt.

## Run locally with DDEV

From the project root:

```bash
ddev start
ddev launch / 
```

You can also request:

```bash
ddev launch /health
```
